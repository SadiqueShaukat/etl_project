{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ETL Pipeline",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/run_pipeline.py",
            "console": "integratedTerminal"
        }
    ]
}